syntax = "proto3";

package seerep;

import "header.proto";

message TimeInterval
{
  Header header = 1;

  // minimum timestamp in unix nanoseconds
  int64 time_min = 2;

  // maximum timestamp in unix nanoseconds
  int64 time_max = 3;
}
